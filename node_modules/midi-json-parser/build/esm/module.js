import { load } from 'midi-json-parser-broker';
import { worker } from './worker/worker';
var blob = new Blob([worker], { type: 'application/javascript; charset=utf-8' });
var url = URL.createObjectURL(blob);
var midiJsonParser = load(url);
export var connect = midiJsonParser.connect;
export var disconnect = midiJsonParser.disconnect;
export var parseArrayBuffer = midiJsonParser.parseArrayBuffer;
//# sourceMappingURL=/build/es2015/module.js.map