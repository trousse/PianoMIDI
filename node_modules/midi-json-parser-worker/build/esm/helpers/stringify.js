/**
 * This function turns a part of a given ArrayBuffer into a String.
 */
export var stringify = function (dataView, offset, length) {
    if (offset === void 0) { offset = 0; }
    if (length === void 0) { length = dataView.byteLength - (offset - dataView.byteOffset); }
    var byteOffset = offset + dataView.byteOffset;
    var array = new Uint8Array(dataView.buffer, byteOffset, length);
    return String.fromCharCode.apply(null, array);
};
//# sourceMappingURL=/build/es2015/helpers/stringify.js.map