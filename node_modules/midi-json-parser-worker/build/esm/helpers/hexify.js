/**
 * This function turns a part of a given ArrayBuffer into a hexadecimal String.
 */
export var hexify = function (dataView, offset, length) {
    if (offset === void 0) { offset = 0; }
    if (length === void 0) { length = dataView.byteLength - (offset - dataView.byteOffset); }
    var byteOffset = offset + dataView.byteOffset;
    var hexArray = [];
    var uint8Array = new Uint8Array(dataView.buffer, byteOffset, length);
    for (var i = 0; i < length; i += 1) {
        var hex = uint8Array[i]
            .toString(16)
            .toUpperCase();
        if (hex.length === 1) {
            hex = 0 + hex;
        }
        hexArray[i] = hex;
    }
    return hexArray.join('');
};
//# sourceMappingURL=/build/es2015/helpers/hexify.js.map